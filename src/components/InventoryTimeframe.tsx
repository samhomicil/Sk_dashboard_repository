// Kept as a named entry point for the inventory module; the control itself is shared with
// the employee module (and anything else that needs a period), so it lives in Timeframe.tsx.
export { default } from './Timeframe'
