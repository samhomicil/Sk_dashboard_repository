# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
cache.json

# Sigma MCP patch files (temp)
data/sigma-patch.json
data/mcp-patch.json

# pfg_extractor: local-only state/output, never needed in the cloud routine
# (which uses Azure SQL watermarks instead) or in this repo's history
src/pfg_extractor/pfg_state.json
src/pfg_extractor/out/
src/pfg_extractor/credentials.json
src/pfg_extractor/token.json
*.sigmabak
